<p>
<% if course.name!='' %>
	<%= link_to course.name, course.weblink %>
	<%= '  ' %>
	<%= link_to 'Destroy Course', [course.user, course],
						:confirm => 'Are you sure you want to delete this course?',
						:method => :delete	 %>
	<% if course.updated %>
		No new updates!
	<% else %>
		Course Website has changed!
	<% end %>
<% end %>
</p>
